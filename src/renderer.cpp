#include "renderer.hpp"
using namespace rayster;

void renderer::render(vector4 a, vector4 b, vector4 c, shader& s) {

}

void renderer::render(const model& m, shader& s) {
    for (int i = 0; i < m.num_faces(); ++i) {
        
    }
}